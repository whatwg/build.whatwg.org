{
  "private": true,
  "name": "wattsi-build",
  "description": "A build server for running wattsi",
  "version": "1.0.0-pre",
  "author": "Domenic Denicola <d@domenic.me> (https://domenic.me/)",
  "license": "WTFPL",
  "repository": "domenic/wattsi-build",
  "bin": "lib/app.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "lint": "eslint lib",
    "start": "forever start lib/app.js -l forever.log --append --watch"
  },
  "dependencies": {
    "child-process-promise": "^1.1.0",
    "co-multipart": "^2.0.0",
    "finished": "^1.2.2",
    "forever": "^0.15.1",
    "koa": "^1.1.2",
    "mkdirp-then": "^1.1.0",
    "rimraf-then": "^1.0.0",
    "then-fs": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^1.10.3"
  }
}
